const $=s=>document.querySelector(s);
const api=path=>fetch(path);
function openCreate(){ $('#modal').classList.remove('hidden'); }
function closeCreate(){ $('#modal').classList.add('hidden'); }
async function loadAgreements(){
  const res=await api('/api/agreements'); if(res.status===401){location.href='/login.html';return;}
  const data=await res.json(); $('#total').textContent=data.length; $('#awaiting').textContent=data.filter(x=>x.status==='AWAITING_CANDIDATE').length; $('#consultAwaiting').textContent=data.filter(x=>x.status==='AWAITING_CONSULTANT').length; $('#complete').textContent=data.filter(x=>x.status==='FULLY_SIGNED').length;
  $('#rows').innerHTML=data.map(a=>`<tr><td><b>${esc(a.agreementNumber)}</b></td><td>${esc(a.candidateFullName)}</td><td>${esc(a.totalFeeAmount)}</td><td>${badge(a.candidateSigningStatus)}</td><td>${badge(a.consultantSigningStatus)}</td><td class="status">${a.valid?'<span class="badge ok">✓ VALID</span>':'<span class="badge pending">Not Valid Yet</span>'}</td><td class="actions-cell"><button class="ghost" type="button" onclick="showSigningLinks('${a.id}')">Links</button><a class="ghost" href="/api/agreements/${a.id}/pdf" target="_blank">PDF</a><button class="danger" type="button" onclick="deleteForConsultant('${a.id}','${esc(a.agreementNumber)}')">Delete</button></td></tr>`).join('');
}
function badge(v){return `<span class="badge ${v==='SIGNED'?'ok':'pending'}">${v==='SIGNED'?'✓ Signed':'Pending'}</span>`}
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
async function showSigningLinks(id){try{const res=await api('/api/agreements/'+id);if(!res.ok)throw new Error();const a=await res.json();const base=location.origin;alert('Client signing link:\n'+base+'/sign.html?party=candidate&token='+a.candidateSigningToken+'\n\nConsultant signing link:\n'+base+'/sign.html?party=consultant&token='+a.consultantSigningToken);}catch(e){alert('Could not load signing links. Please refresh and try again.');}}
async function deleteForConsultant(id,agreementNumber){if(!confirm('Remove '+agreementNumber+' from the Consultant dashboard only?\n\nThe agreement will remain stored and signing links will continue to work.'))return;try{const res=await fetch('/api/agreements/'+id+'/consultant',{method:'DELETE'});const body=await res.json().catch(()=>({}));if(!res.ok)throw new Error(body.message||'Could not remove agreement');await loadAgreements();alert('Agreement removed from the Consultant dashboard.');}catch(e){alert(e.message||'Could not remove agreement');}}
$('#createForm').addEventListener('submit',async e=>{e.preventDefault();const form=e.target;const obj=Object.fromEntries(new FormData(form).entries());const button=form.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Generating agreement PDF…';try{const res=await fetch('/api/agreements',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(obj)});if(!res.ok){const error=await res.json().catch(()=>({}));throw new Error(error.message||'Could not create agreement');}const a=await res.json();button.textContent='Sending agreement to client…';const sendRes=await fetch('/api/agreements/'+a.id+'/send',{method:'POST'});if(!sendRes.ok){const error=await sendRes.json().catch(()=>({}));throw new Error(error.message||'Client email could not be sent');}closeCreate();form.reset();await loadAgreements();const base=location.origin;alert('Agreement generated and sent to the client.\n\nClient signing link:\n'+base+'/sign.html?party=candidate&token='+a.candidateSigningToken+'\n\nThe client can view/download the exact generated PDF, review it and complete Aadhaar OTP eSign.\n\nThe consultant will be notified only after the client signs.');}catch(e){alert(e.message||'Could not create the agreement.');}finally{button.disabled=false;button.textContent='Generate Agreement & Email Client';}});
loadAgreements();
