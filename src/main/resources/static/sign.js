const params=new URLSearchParams(location.search);const party=params.get('party');const token=params.get('token');let agreement;const $=s=>document.querySelector(s);
function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'})[m]);}
function show(t,c){$('#notice').textContent=t;$('#notice').className='notice '+(c||'');}
function d(k,v){return `<div class="detail"><span>${esc(k)}</span><strong>${esc(v)}</strong></div>`;}
function renderDetails(){$('#details').innerHTML=`${d('Agreement Number',agreement.agreementNumber)}${d('Client',agreement.candidateFullName)}${d('Execution Date',agreement.executionDate)}${d('Total Fee',agreement.totalFeeAmount)}${d('Pre-Payment',agreement.prePaymentAmount)}${d('Balance',agreement.balanceAmount)}${d('Agreement Status',agreement.status)}${d('Client Signature',agreement.candidateSigningStatus)}${d('Consultant Signature',agreement.consultantSigningStatus)}${d('Agreement Valid',agreement.valid?'YES - Fully Executed':'NO - Both Signatures Required')}`;}
function updateSigningStatus(){if(!agreement)return;const already=party==='candidate'?agreement.candidateSigningStatus==='SIGNED':agreement.consultantSigningStatus==='SIGNED';const btn=$('#signBtn');if(agreement.status==='FULLY_SIGNED'){btn.disabled=true;btn.textContent='✓ Agreement Fully Executed';$('#signStatus').innerHTML='<div class="success-status">✓ Both parties have signed. This agreement is fully executed and valid.</div>';return;}if(already){btn.disabled=true;btn.textContent='✓ Already Signed';$('#signStatus').innerHTML='<div class="success-status">✓ Your acceptance/signature has already been recorded.</div>';return;}if(party==='consultant'&&agreement.candidateSigningStatus!=='SIGNED'){btn.disabled=true;btn.textContent='Waiting for Client Signature';$('#signStatus').innerHTML='<div class="waiting-status">Client review and acceptance is required before consultant signing can proceed.</div>';return;}btn.disabled=false;btn.textContent='✓ I Accept the Agreement & Sign';}
async function init(){if(!party||!token||!['candidate','consultant'].includes(party)){show('Invalid signing link.','error');return;}try{const base=`/api/sign/${encodeURIComponent(party)}/${encodeURIComponent(token)}`;const r=await fetch(base);if(!r.ok){show('This signing link is invalid or expired.','error');return;}agreement=await r.json();$('#heading').textContent=agreement.agreementNumber;$('#partyName').textContent=party==='candidate'?agreement.candidateFullName:'PV Talent Partners';$('#partyText').textContent=party==='candidate'?'You are signing as the Client.':'You are signing as the Consultant / Proprietor.';renderDetails();const pdfUrl=`${base}/pdf`;$('#pdfViewer').src=pdfUrl;$('#downloadPdf').href=pdfUrl;updateSigningStatus();}catch(e){console.error(e);show('Unable to load the agreement.','error');}}
async function acceptSign(){if(!agreement){show('Agreement is not loaded.','error');return;}if(!$('#consent').checked){show('Please review the complete agreement and accept the Terms & Conditions before signing.','error');return;}if(party==='consultant'&&agreement.candidateSigningStatus!=='SIGNED'){show('Client must accept the agreement before you can sign.','error');return;}const button=$('#signBtn');button.disabled=true;button.textContent='Recording acceptance…';try{const response=await fetch(`/api/agreements/${agreement.id}/accept?party=${encodeURIComponent(party)}`,{method:'POST',headers:{'Content-Type':'application/json','X-Signing-Token':token},body:JSON.stringify({consent:true})});const data=await response.json();if(!response.ok)throw new Error(data.message||'Unable to record acceptance.');agreement=data;renderDetails();updateSigningStatus();if(agreement.status==='FULLY_SIGNED')show('✓ Agreement fully signed by both parties and is now valid.','success');else show('✓ Acceptance recorded. The consultant has been notified to review and sign.','success');}catch(e){console.error(e);show(e.message||'Unable to record acceptance.','error');button.disabled=false;button.textContent='✓ I Accept the Agreement & Sign';}}
init();
